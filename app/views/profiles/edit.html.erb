<h1>Edit Profile</h1>

<%= render 'partials/form', profile: @profile %>

<%= link_to 'Back', profile_path(@profile) %> | <%= link_to 'Delete', profile_path(@profile), method: :delete, data: { confirm: 'Are you sure?' } %>